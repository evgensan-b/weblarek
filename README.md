# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
Используются интерфейсы данных, которые были разработаны при разборе проекта:

#### IProduct
Интерфейс, описывающий карточку товара в каталоге магазина. Включает в себя поля:
- `id: string` - уникальный идентификатор товара.
- `description: string` - описание товара.
- `image: string` - путь к изображению товара.
- `title: string` - наименование товара.
- `category: string` - категория товара.
- `price: number | null` - цена товара (может быть null, если товар недоступен).

#### IBuyer
Интерфейс, описывающий данные покупателя при оформлении заказа. Включает в себя поля:
- `payment: TPayment` - способ оплаты заказа.
- `email: string` - электронная почта покупателя.
- `phone: string` - телефон покупателя.
- `address: string` - адрес доставки заказа.

#### TPayment
Тип способа оплаты заказа. Может принимать значения:
- `'card'` - оплата картой.
- `'cash'` - оплата наличными.
- `''` - вариант, когда пользователь еще не выбрал способ оплаты.

#### BuyerValidationErrors
Тип для валидации ошибок данных покупателя. Используется в методе `validBuyerData()` класса `Buyer`. Тип: `Partial<Record<keyof IBuyer, string>>`. Описание: 
- Объект, где ключи - только поля из `IBuyer`.
- Значения - тексты ошибок для соответствующих полей.
- Поля опциональны (если ошибки нет - поля нет в объекте). 
- Пример: `{ email: 'Укажите емэйл', phone: 'Укажите номер телефона' }`.

#### IProductListResponse
Интерфейс, описывающий ответ API сервера при запросе списка товаров. Включает в себя поля:
- `total: number` - общее количество товаров.
- `items: IProduct[]` - массив товаров.

#### IOrderData
Интерфейс, описывающий данные заказа для отправки на сервер при оформлении покупки. Расширяет интерфейс `IBuyer`, наследуя все его поля, а также включает в себя дополнительные поля:
- `total: number` - общая стоимость созданного заказа.
- `items: string[]` - массив идентификаторов товаров в заказе.

#### IOrderResponse
Интерфейс, описывающий ответ сервера после успешного оформления заказа. Включает в себя поля:
- `id: string` - уникальный идентификатор созданного заказа.
- `total: number` - общая стоимость созданного заказа.

### Модели данных
Для учёта данных в приложении существует три класса, которые будут разделены между собой по смыслу и зонам ответственности:
- хранение товаров, которые можно купить в приложении;
- хранение товаров, которые пользователь выбрал для покупки;
- данные покупателя, которые тот должен указать при оформлении заказа.

#### ProductCatalog
Класс модели данных для хранения каталога товаров интернет-магазина и управления выбранным для просмотра товаром.
**Назначение:** хранение всех товаров магазина и выбранного товара для детального просмотра
##### Конструктор класса:
`constructor()` - конструктор не принимает параметров.
##### Поля класса:
- `items: IProduct[]` - хранит массив всех товаров в каталоге магазина;
- `preview: IProduct | null` - хранит товар, выбранный для детального просмотра (может быть null);
##### Методы класса:
- `setItems(items: IProduct[]): void` - сохранение массива товаров полученного в параметрах метода;
- `getItems(): IProduct[]` - получение массива товаров из модели;
- `getProductById(id: string): IProduct | null` - получение одного товара по его id;
- `setPreview(product: IProduct): void` - сохранение товара для подробного отображения;
- `getPreview(): IProduct | null` - получение товара для подробного отображения

#### Basket
Класс модели данных для управления корзиной покупок.
**Назначение:** хранение товаров, выбранных покупателем для покупки, и управление корзиной
##### Конструктор класса:
`constructor()` - конструктор не принимает параметров.
##### Поля класса:
- `items: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки и находящихся в корзине
##### Методы класса:
- `getItems():IProduct[]` - получение массива товаров, которые находятся в корзине;
- `addItem(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины;
- `removeItem(productId: string): void` - удаление товара, полученного в параметре из массива корзины;
- `clear(): void` - очистка корзины;
- `getTotalPrice(): number` - получение стоимости всех товаров в корзине;
- `getCount(): number` - получение количества товаров в корзине;
- `hasProduct(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода

#### Buyer
Класс модели данных для хранения и валидации данных покупателя.
**Назначение:** хранение данных покупателя при оформлении заказа и валидация этих данных
##### Конструктор класса:
`constructor()` - конструктор не принимает параметров.
##### Поля класса:
- `payment: TPayment | null` - способ оплаты (может быть null);
- `address: string` - адрес доставки заказа;
- `phone: string` - телефон покупателя;
- `email: string` - электронная почта покупателя
##### Методы класса:
- `setBuyerData(data: Partial<IBuyer>): void` - сохранение данных в модели;
- `getBuyerData(): IBuyer` - получение всех данных покупателя;
- `clearBuyerData(): void` - очистка данных покупателя;
- `validBuyerData(): BuyerValidationErrors` - валидация данных покупателя;

### Слой коммуникации
Раздел, отвечающий за получение данных с сервера и отправку данных на сервер

#### WebLarekAPI
Класс модели данных для взаимодействия с API сервера «веб-ларёк». Этот класс будет использовать композицию, чтобы выполнить запрос на сервер с помощью метода get класса Api и будет получать с сервера объект с массивом товаров.
**Назначение:** отвечает за получение данных с сервера и отправку данных на сервер
##### Конструктор класса:
`constructor(api: IApi)` - принимает экземпляр класса, реализующего интерфейс IApi.
##### Поля класса:
- `api: IApi` - хранит данные запроса
##### Методы класса:
- `getProductList(): Promise<IProduct[]>` - делает get запрос на эндпоинт /product/ и возвращает массив товаров;
- `orderProducts(orderData: IOrderData): Promise<IOrderResponse>` - делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода

### Слой Представления (View)
Раздел, отвечающий за отображение и внешний вид приложения

#### Header
Класс представления, который отвечает за отображение блока разметки шапки сайта с кнопкой корзины и счетчиком товаров в корзине. **Назначение:** отвечает за отображение счетчика товаров в корзине и обработку клика по кнопке корзины
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры объект, через который будет инициироваться событие клика по корзине.
##### Поля класса:
- `basketButton: HTMLButtonElement` - элемент кнопки корзины, используемый для открытия корзины;
- `counterElement: HTMLElement` - элемент счетчика товаров в корзине
##### Методы класса:
- `set counter(value: number): void` - устанавливает значение счетчика товаров в корзине для отображения

#### Gallery
Класс представления, который отвечает за отображение блока разметки каталога с карточками товаров. **Назначение:** отвечает за отображение каталога товаров с карточками товаров на главной странице сайта
##### Конструктор класса:
`constructor(container: HTMLElement)` - принимает в параметры элемент, который является контейнером для отображения каталога товаров.
##### Поля класса:
- `catalogElement: HTMLElement` - элемент, внутри которого будут размещаться карточки товаров.
##### Методы класса:
- `set catalog(items: HTMLElement[]): void` - устанавливает массив карточек товара и вставляет их в разметку для отображения

#### Modal
Класс представления, который отвечает за отображение блока разметки модального окна.**Назначение:** отвечает за отображение модального окна с возможным контентом и обработку закрытия модального окна.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры объект, через который будет инициироваться событие клика по кнопке закрытия модального окна.
##### Поля класса:
- `closeButton: HTMLButtonElement` - элемент кнопки модального окна, используемый для закрытия модального окна;
- `contentElement: HTMLElement` - элемент в котором размещается контент модального окна
##### Методы класса:
- `set content(value: HTMLElement): void` - устанавливает содержимое модального окна переданным элементом

#### Success
Класс представления, который отвечает за отображения блока разметки подтверждающего успешное оформление заказа.**Назначение:** отвечает за отображение сообщения об успешном оформлении заказа с общей суммой списания за оформленный заказ.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры объект, через который будет инициироваться событие клика по кнопке закрытия экрана подтверждения успешного оформления заказа.
##### Поля класса:
- `closeButton: HTMLButtonElement` - элемент кнопки, используемый для закрытия экрана подтверждения успешного оформления заказа;
- `description: HTMLElement` - элемент, который отображает общую сумму списания за оформленный заказ
##### Методы класса:
- `set total(value: number): void` - устанавливает общую сумму списания за оформленный заказ

#### Card
Базовый (Абстрактный) класс представления, который является родительским классом для классов, работающих с различными типами или темплейтами карточек товаров. Содержит функционал, общий для всех своих дочерних классов.**Назначение:** отвечает за отображение базовой информации о товаре (заголовок(наименование), стоимость), а также предоставляет основу для расширения и создания других специализированных типов карточек товаров.
##### Конструктор класса:
`constructor(container: HTMLElement)` - принимает в параметры контейнер карточки товара.
##### Поля класса:
- `cardTitle: HTMLElement` - элемент, который отображает заголовок товара;
- `cardPrice: HTMLElement` - элемент, который отображает стоимость товара
##### Методы класса:
- `set title(value: string): void` - устанавливает заголовок(наименование) товара;
- `set price(value: number | null): void` - устанавливает значение цены товара. Если цена товара не указана, отображает "Бесценно"

#### CardCatalog
Класс представления, который является дочерним классом для класса `Card`.**Назначение:** отвечает за отображение карточки товара в каталоге товаров на главной странице сайта и включает в себя дополнительную информацию о товаре(изображение, категория).
##### Конструктор класса:
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает в параметры элемент, который является карточкой товара и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `cardImage: HTMLImageElement` - элемент, который выводит изображение товара;
- `cardCategory: HTMLElement` - элемент, который отображает категорию товара
##### Методы класса:
- `set image(src: string): void` - устанавливает путь к изображению товара;
- `set category(value: string): void` - устанавливает значение категории товара

#### CardPreview
Класс представления, который является дочерним классом для класса `Card`.**Назначение:** отвечает за подробное отображение карточки товара, а также включает в себя кнопку добавления или удаления товара в корзине.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры элемент, который является подробной карточкой товара и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `cardImage: HTMLImageElement` - элемент, который выводит изображение товара;
- `cardCategory: HTMLElement` - элемент, который отображает категорию товара;
- `cardDescription: HTMLElement` - элемент, который отображает подробное описание товара;
- `cardButton: HTMLButtonElement` - элемент кнопки карточки товара, используемый для добавления или удаления товара из корзины
##### Методы класса:
- `set image(src: string): void` - устанавливает путь к изображению товара;
- `set category(value: string): void` - устанавливает значение категории товара;
- `set description(value: string): void` - устанавливает подробное описание товара;
- `set buttonText(value: string): void` - устанавливает текстовое значение кнопки добавления или удаления товара из корзины;
- `set disabled(value: boolean): void` - блокирует/разблокирует кнопку добавления или удаления товара из корзины

#### CardBasket
Класс представления, который является дочерним классом для класса `Card`.**Назначение:** отвечает за компактное отображение карточки товара в корзине, а также включает в себя кнопку удаления товара из корзины.
##### Конструктор класса:
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает в параметры элемент, который является карточкой товара и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `cardIndex: HTMLElement` - элемент, который отображает порядковый номер товара в корзине;
- `cardButtonBasket: HTMLButtonElement` - элемент, используемый для удаления товара из корзины
##### Методы класса:
- `set index(value: number): void` - устанавливает порядковый номер товара в корзине

#### Basket
Класс представления, который отвечает за отображение блока разметки корзины товаров.**Назначение:** отвечает за отображение списка товаров в корзине, общей стоимости товаров и кнопки оформления заказа.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры контейнер корзины товаров и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `basketList: HTMLElement` - элемент, который отображает список товаров;
- `basketTotal: HTMLElement` - элемент, который отображает общую стоимость товаров;
- `basketButton: HTMLButtonElement` - элемент, используемый для оформления заказа
##### Методы класса:
- `set items(items: HTMLElement[]): void` - устанавливает список товаров в корзине. Если список пуст, отображает уведомление о пустой корзине;
- `set total(value: number): void` - устанавливает общую стоимость товаров в корзине;
- `set disabled(value: boolean): void` - блокирует/разблокирует кнопку оформления заказа

#### Form
Базовый (Абстрактный) класс представления, служащий основой для всех классов, представляющих различные формы ввода данных. Содержит общую логику для обработки событий ввода, валидации полей и управления процессом отправки формы..**Назначение:** обеспечивает единый подход к управлению формами, их состоянием и взаимодействием с пользователем.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры контейнер содержащий разметку формы и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `formError: HTMLElement` - элемент, который содержит сообщение об ошибке;
- `formButton: HTMLButtonElement` - элемент, отвечающий за иницирование отправки формы
##### Методы класса:
- `set error(value: string): void` - отображает сообщение об ошибке валидации;
- `set valid(value: boolean): void` - активирует/деактивирует кнопку отправки в зависимости от валидности формы

#### FormOrder
Класс представления, который является дочерним классом для класса `Form`.**Назначение:** отвечает за шаг формы оформления заказа - выбор способа оплаты и адреса доставки заказа.
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры элемент, который является контейнером формы и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `formOrderButtons: Record<TPayment, HTMLButtonElement>` - элементы, которые предоставляют выбор способы оплаты заказа;
- `formAddress: HTMLInputElement` - элемент, используемый для поля ввода адреса доставки заказа
##### Методы класса:
- `set payment(value: TPayment | null): void` - устанавливает способ оплаты заказа;
- `set address(value: string): void` - устанавливает введенный адресс доставки заказа

#### FormСontacts
Класс представления, который является дочерним классом для класса `Form`.**Назначение:** отвечает за шаг формы оформления заказа - ввод контактных данных покупателя(email, телефон).
##### Конструктор класса:
`constructor(events: IEvents, container: HTMLElement)` - принимает в параметры элемент, который является контейнером формы и будет использоваться для обработки пользовательских действий.
##### Поля класса:
- `formEmail: HTMLInputElement` - элемент, используемый для поля ввода email покупателя;
- `formPhone: HTMLInputElement` - элемент, используемый для поля ввода телефона покупателя
##### Методы класса:
- `set email(value: string): void` - устанавливает введенный email покупателя;
- `set phone(value: string): void` - устанавливает введенный телефон покупателя

### Слой Событий
Раздел, отвечающий за описание событий, которые генерируются в приложении

#### basket:open
Событие, которое генерируется классом `Header` при клике на кнопку корзины. Сигнализирует о намерении пользователя открыть корзину.

#### modal:close
Событие, которое генерируется классом `Modal` при закрытии модального окна(клик на кнопку закрытия, клик вне модального окна или нажатием на клавишу Escape). Сигнализирует о намерении пользователя закрыть модальное окно.

#### product:toggle-cart
Событие, которое генерируется классом `CardPreview` при клике на кнопку "Купить"/"Удалить из корзины" на подробной карточке отображения товара. Сигнализирует о намерении пользователя добавить/удалить товар из корзины.